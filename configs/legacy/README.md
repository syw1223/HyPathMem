# Legacy Configurations

These files document superseded experiments and are not part of the paper
mainline. In particular, `qa_poloai_gpt4omini.yaml` used GPT-4o-mini for both
generation and judging with a Top-5 graph output. The reported main tables use
GPT-4.1-mini or Qwen3-30B for generation and GPT-4o-mini for one judgment per
answer.
